---
description: POM 截图流水线门禁——Todo 驱动分步执行、失败即停、先自检后用例
alwaysApply: true
---

# POM 流水线门禁

涉及 **截图 → 页面对象 → 自动化测试** 时，必须遵循 Skill `pom-screenshot-pipeline` 与 **`pom-todo-progress.mdc`（Todo 驱动，最高优先级）**。

## Todo 驱动（不可违反）

1. **第一个工具调用**：`TodoWrite(merge=false)` 创建标准 12 项清单
2. **每阶段**：`in_progress` → 只做该阶段工作 → `completed` → 下一阶段
3. **禁止静默连跑**：不得在未更新 Todo 的情况下连续完成多个阶段
4. **进度展示**：用户在 Cursor 任务面板查看，Agent 不得仅用最终文字代替 Todo 进度

## 一张截图完整交付

```
截图 → page_object → 关联代码 → 自检 → 用例/双份YAML → pytest → Allure → bump
```

每项对应一个 Todo id，**逐项推进**，不得跳步。

## 执行顺序与 Todo 映射

> 下表 **12 行 = 12 个独立 Todo**，每项单独 `in_progress → completed`，**禁止**合并执行。

| 顺序 | Todo id | 动作 |
|------|---------|------|
| 1 | `analyze` | 分析截图/需求 |
| 2 | `code-scan` | 扫描已有代码模板 |
| 3 | `page-object` | 生成页面对象 |
| 4 | `validate-data` | 生成 validate YAML |
| 5 | `registry` | 注册 `PAGE_REGISTRY` |
| 6 | `self-check` | `validate_page_object.py`（失败即停） |
| 7 | `test-data` | 生成正式 YAML |
| 8 | `fixture` | conftest 注册 fixture |
| 9 | `test-case` | 生成测试用例 |
| 10 | `pytest-run` | pytest（失败即停） |
| 11 | `allure-report` | 生成 Allure 报告 |
| 12 | `bump-data` | 幂等递增唯一字段 |

## 失败即停

- 失败 Todo 保持 `in_progress` / blocked
- 后续 Todo 保持 `pending`，**不得继续执行**
- 展示：Todo id、traceback、截图/DOM 路径

## 代码关联（必检）

| 文件 | 动作 |
|------|------|
| `conftest.py` | `{module}_page` fixture |
| `validate_page_object.py` | `PAGE_REGISTRY` |
| `login.yaml` | 复用登录 parametrize |
| 同类 `page_object/*.py` | 定位器风格一致 |
