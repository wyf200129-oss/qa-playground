[Test_Env]
# 测试环境地址（替换为实际地址）
host = http://[ERP_TEST_HOST]:[ERP_TEST_PORT]/

[Dev_Env]
# 开发环境地址（替换为实际地址）
host = http://[ERP_DEV_HOST]:[ERP_DEV_PORT]/

[data]
# 运行时 token 缓存（登录成功后自动写入，用例结束后自动清空）
token =
